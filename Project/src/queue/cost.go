// Calculating cost for all elevators
package queue

/*
import(
	"../driver"
)

func Cost (currFloor, targetFloor, direction int) int{
	cost := 0
	if currFloor == -1 { //Hvis heisen er mellom etasjer
		cost++
	}
	else if direction != 0 { //Heis på etasje, men i full fart
		cost += 2
	}
	if currFloor < targetFloor {
		for floor := currFloor; floor <= N_FLOORS; floor++ {
			cost++
		}
		if direction < 0 {
			cost += 5
		}
	}
	if currFloor > targetFloor {
		for floor := N_FLOORS; floor => currFloor; floor-- {
			cost++
		}
		if direction > 0 {
			cost += 5
		}
	}
	return cost
}
*/
